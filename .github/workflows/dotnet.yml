name: .NET

on:
  push:
    branches: [ main ]
    paths:
      - 'ServicesOracle/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'ServicesOracle/**'

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      working-directory: ./ServicesOracle
      run: dotnet restore
    - name: Build
      working-directory: ./ServicesOracle
      run: dotnet build --no-restore
    - name: Test
      working-directory: ./ServicesOracle
      run: dotnet test --no-build --verbosity normal